# 🧔 BORODACH Franchise Bot

Telegram-бот для франчайзи сети барбершопов BORODACH. Позволяет создавать задачи в управляющую компанию через Bitrix24, управлять филиалами и просматривать статистику из YClients.

---

## 📋 Оглавление

- [Бизнес-логика](#бизнес-логика)
- [Возможности бота](#возможности-бота)
- [Система верификации партнёров](#система-верификации-партнёров)
- [Админ-панель](#админ-панель)
- [Структура проекта](#структура-проекта)
- [База данных](#база-данных)
- [Установка и настройка](#установка-и-настройка)
- [Переменные окружения](#переменные-окружения)
- [Запуск](#запуск)
- [Интеграция с Bitrix24](#интеграция-с-bitrix24)
- [Интеграция с YClients](#интеграция-с-yclients)
- [Сценарии использования](#сценарии-использования)
- [Технические детали](#технические-детали)

---

## 🎯 Бизнес-логика

### Цель бота

Дать франчайзи простой инструмент для:
- **Создания задач** в управляющую компанию (УК)
- **Просмотра своих задач** и их статусов
- **Отмены задач** при необходимости

### Пользователи

- **Франчайзи (Партнёры)** — владельцы/управляющие филиалов BORODACH
- Требуется **верификация** через админ-панель
- Привязка к филиалам — через систему с yclients_id
- Возможность управлять несколькими филиалами

### Отделы УК

Бот поддерживает постановку задач в три отдела:

| Отдел | Описание | Emoji |
|-------|----------|-------|
| 🚀 Отдел Развития | Вопросы развития бизнеса, операционные задачи | 🚀 |
| 📢 Отдел Маркетинга | Реклама, продвижение, маркетинговые материалы | 📢 |
| 🎨 Дизайн | Дизайн материалов, оформление | 🎨 |

Каждому отделу соответствует отдельный проект в Bitrix24.

---

## ✨ Возможности бота

### 1. Создание задачи

**Полный flow:**
1. Выбор отдела (Развитие / Маркетинг / Дизайн)
2. Ввод филиала (город / ТЦ / адрес)
3. Ввод названия задачи (краткий заголовок)
4. Ввод описания задачи
5. Подтверждение или добавление комментария
6. Прикрепление файлов (опционально)
7. Создание задачи в Bitrix24

**Что сохраняется в Bitrix:**
- Название: `[Филиал] Заголовок задачи`
- Описание с информацией об отправителе
- Прикреплённые файлы (загружаются в Bitrix Disk)
- TG_USER_ID для идентификации автора

### 2. Просмотр задач

**«Мои задачи»** — показывает только задачи в работе:
- Группировка по отделам
- Внутри отдела — группировка по этапам Kanban
- Сортировка этапов: Новая → Выполняются → На проверке

**«Все задачи»** — включает завершённые и отменённые:
- Дополнительно показывает этапы: Завершена, Отменена

### 3. Отмена задачи

- Доступна для задач в работе
- Нельзя отменить завершённую задачу
- Нельзя отменить уже отменённую задачу
- Требует подтверждения
- Переводит задачу на этап «Отменена» в Kanban

---

## 🔐 Система верификации партнёров

### Процесс регистрации

1. Пользователь нажимает "📝 Зарегистрироваться"
2. Вводит ФИО
3. Делится контактом через Telegram (кнопка "Поделиться контактом")
4. Вводит название филиала(ов) свободным текстом
5. Получает уведомление об отправке заявки
6. Ждёт верификации (кнопка "🔄 Проверить статус")

### Статусы партнёра

| Статус | Описание |
|--------|----------|
| `pending` | Ожидает верификации |
| `verified` | Верифицирован, доступ к боту открыт |
| `rejected` | Отклонён, может подать повторно |

### После верификации

- Партнёр получает уведомление в боте
- Открывается главное меню: **Задачи**, **Мои филиалы**, **Статистика**
- Можно добавлять новые филиалы (требует повторной верификации)

### При удалении партнёра

- Партнёр получает уведомление об удалении
- Доступ к функциям блокируется
- Появляется кнопка регистрации заново

---

## 🖥 Админ-панель

Веб-панель для управления партнёрами и филиалами.

### Доступ

- **URL:** `http://IP:8000` или через поддомен (напр. `adminbot.borodach.com`)
- **Авторизация:** логин/пароль из переменных окружения

### Функции

#### Партнёры
- Просмотр заявок на регистрацию (pending)
- Верификация с привязкой к филиалам
- Отклонение заявок с уведомлением
- Просмотр верифицированных партнёров
- Удаление партнёров

#### Филиалы
- Добавление новых филиалов (название + yclients_id)
- Редактирование филиалов
- Удаление филиалов

#### Рассылка
- Отправка сообщений всем верифицированным партнёрам
- Выбор конкретных получателей (чекбоксы)
- WYSIWYG редактор (жирный, курсив, подчёркнутый, код, эмодзи)
- История рассылок с датой, текстом и количеством получателей

### Запуск админ-панели

```bash
# На сервере
source venv/bin/activate
nohup python run_admin.py > logs/admin.log 2>&1 &
```

### Nginx (для поддомена)

```nginx
server {
    listen 80;
    server_name adminbot.borodach.com;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

---

## 📁 Структура проекта

```
borodach-franchise-bot/
│
├── main.py                 # Точка входа, запуск бота
├── run_admin.py            # Запуск админ-панели
├── requirements.txt        # Зависимости Python
├── README.md               # Документация (этот файл)
├── alembic.ini             # Конфигурация миграций БД
│
├── bot/                    # Модуль Telegram-бота
│   ├── __init__.py         # Экспорт роутеров
│   ├── handlers.py         # Обработчики сообщений и команд
│   ├── keyboards.py        # Клавиатуры (Reply)
│   ├── registration.py     # FSM регистрации партнёров
│   └── utils.py            # Вспомогательные функции
│
├── admin/                  # Админ-панель (FastAPI)
│   ├── __init__.py
│   ├── app.py              # FastAPI приложение
│   ├── auth.py             # Аутентификация
│   ├── routes.py           # API endpoints
│   └── templates/          # HTML шаблоны (Jinja2)
│       ├── base.html
│       ├── login.html
│       ├── dashboard.html
│       ├── partners.html
│       ├── branches.html
│       ├── verify_partner.html
│       ├── edit_branch.html
│       └── broadcast.html
│
├── database/               # Модуль базы данных
│   ├── __init__.py
│   ├── connection.py       # Подключение к PostgreSQL
│   ├── models.py           # SQLAlchemy модели
│   └── crud.py             # CRUD операции
│
├── migrations/             # Миграции Alembic
│   ├── env.py
│   ├── script.py.mako
│   └── versions/           # Файлы миграций
│
├── bitrix/                 # Модуль интеграции с Bitrix24
│   ├── __init__.py
│   ├── client.py           # REST-клиент для Bitrix API
│   └── tasks.py            # Функции работы с задачами
│
├── yclients/               # Модуль интеграции с YClients
│   ├── __init__.py
│   └── client.py           # API клиент YClients
│
├── config/                 # Конфигурация
│   ├── __init__.py
│   └── settings.py         # Загрузка настроек из .env
│
├── scripts/                # Скрипты управления
│   ├── run.sh              # Запуск бота
│   └── stop.sh             # Остановка бота
│
├── logs/                   # Логи (не в Git)
│   ├── bot.log
│   └── admin.log
│
└── .github/
    └── workflows/
        └── deploy.yml      # CI/CD для автодеплоя
```

---

## 🗄 База данных

### PostgreSQL

Бот использует PostgreSQL для хранения данных о партнёрах и филиалах.

### Модели данных

| Таблица | Описание |
|---------|----------|
| `partners` | Партнёры (telegram_id, имя, телефон, статус) |
| `branches` | Филиалы (название, yclients_id) |
| `partner_branches` | Связь партнёр-филиал (many-to-many) |
| `broadcast_history` | История рассылок |

### Настройка PostgreSQL

```bash
# На сервере (Ubuntu)
sudo -u postgres psql

# Создать БД и пользователя
CREATE DATABASE borodach_franchise;
CREATE USER borodach_bot WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE borodach_franchise TO borodach_bot;
\c borodach_franchise
GRANT ALL ON SCHEMA public TO borodach_bot;
```

### Миграции (Alembic)

```bash
# Создать миграцию
alembic revision --autogenerate -m "description"

# Применить миграции
alembic upgrade head

# Откатить
alembic downgrade -1
```

---

## 🛠 Установка и настройка

### Требования

- Python 3.11+
- Доступ к Bitrix24 (webhook)
- Telegram Bot Token

### Локальная установка

```bash
# Клонировать репозиторий
git clone https://github.com/VGUROV050/borodach-franchise-bot.git
cd borodach-franchise-bot

# Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или venv\Scripts\activate  # Windows

# Установить зависимости
pip install -r requirements.txt

# Создать файл .env
cp .env.example .env
# Заполнить переменные в .env
```

---

## 🔐 Переменные окружения

Создайте файл `.env` в корне проекта:

```env
# Telegram
TELEGRAM_BOT_TOKEN=123456:ABC-DEF...

# Bitrix24 Webhook URL
BITRIX_WEBHOOK_URL=https://your-domain.bitrix24.ru/rest/1/abc123xyz/

# ID проектов в Bitrix24 для каждого отдела
BITRIX_GROUP_ID_DEVELOPMENT=123
BITRIX_GROUP_ID_MARKETING=124
BITRIX_GROUP_ID_DESIGN=125

# ID ответственных сотрудников для каждого отдела
BITRIX_RESPONSIBLE_DEVELOPMENT=1
BITRIX_RESPONSIBLE_MARKETING=2
BITRIX_RESPONSIBLE_DESIGN=3

# База данных PostgreSQL
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/borodach_franchise

# Админ-панель
ADMIN_SECRET_KEY=super-secret-key-change-me
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-password

# YClients API
YCLIENTS_PARTNER_TOKEN=your-partner-token
YCLIENTS_USER_TOKEN=your-user-token
```

### Как получить значения

| Переменная | Где взять |
|------------|-----------|
| `TELEGRAM_BOT_TOKEN` | [@BotFather](https://t.me/BotFather) в Telegram |
| `BITRIX_WEBHOOK_URL` | Bitrix24 → Приложения → Вебхуки → Создать |
| `BITRIX_GROUP_ID_*` | URL проекта в Bitrix: `/workgroups/group/123/` → ID = 123 |
| `BITRIX_RESPONSIBLE_*` | Профиль сотрудника в Bitrix: `/company/personal/user/1/` → ID = 1 |
| `DATABASE_URL` | Формат: `postgresql+asyncpg://user:pass@host:5432/dbname` |
| `ADMIN_*` | Придумать самостоятельно (надёжный пароль!) |
| `YCLIENTS_PARTNER_TOKEN` | YClients → Настройки → API → Партнёрский токен |
| `YCLIENTS_USER_TOKEN` | YClients → Настройки → API → Пользовательский токен |

### Права вебхука Bitrix24

Вебхук должен иметь права:
- `task` — работа с задачами
- `disk` — загрузка файлов

---

## 🚀 Запуск

### Локально

```bash
source venv/bin/activate
python main.py
```

### На сервере (Beget)

```bash
# Запуск
bash scripts/run.sh

# Остановка
bash scripts/stop.sh

# Просмотр логов
tail -f logs/bot.log
```

### Автодеплой

При push в `main` ветку автоматически:
1. Подключается к серверу по SSH
2. Делает `git pull`
3. Перезапускает бота

Настройки в GitHub Secrets:
- `BEGET_HOST` — SSH хост
- `BEGET_USER` — имя пользователя
- `BEGET_PASSWORD` — пароль

---

## 🔗 Интеграция с Bitrix24

### Используемые методы API

| Метод | Описание |
|-------|----------|
| `tasks.task.add` | Создание задачи |
| `tasks.task.list` | Получение списка задач |
| `tasks.task.get` | Получение задачи по ID |
| `tasks.task.update` | Обновление задачи (смена этапа) |
| `task.stages.get` | Получение этапов Kanban |
| `disk.storage.uploadfile` | Загрузка файла |
| `tasks.task.files.attach` | Прикрепление файла к задаче |

### Этапы Kanban

Бот работает с этапами по названию (регистронезависимо):

| Этап | Emoji | Описание |
|------|-------|----------|
| Новая задача | 🆕 | Новые задачи |
| Выполняется | ⏳ | Задачи в работе |
| На проверке | 👀 | Ожидают проверки |
| Завершена | ✅ | Выполненные задачи |
| Отменена | 🚫 | Отменённые задачи |

**Важно:** В каждом проекте Bitrix должен быть этап «Отменена» для работы функции отмены.

---

## 📊 Интеграция с YClients

### Функциональность

- **Статистика по филиалам** — выручка и количество завершённых записей за текущий месяц
- Данные привязаны к филиалам через `yclients_id`

### API Endpoints

| Метод | Описание |
|-------|----------|
| `GET /company/{id}/analytics/overall/` | Основные показатели (выручка, записи) |
| `GET /company/{company_id}` | Информация о филиале |

### Авторизация

YClients требует два токена в заголовке:
```
Authorization: Bearer {PARTNER_TOKEN}, User {USER_TOKEN}
```

### Получение статистики

Используется эндпоинт `/company/{id}/analytics/overall/` с параметрами:
- `date_from` — начало периода (YYYY-MM-DD)
- `date_to` — конец периода (YYYY-MM-DD)

Ответ содержит:
- `income_total_stats.current_sum` — общая выручка
- `record_stats.current_completed_count` — завершённые записи
- `record_stats.current_total_count` — всего записей

### Статус интеграции

✅ **Работает** — выручка и статистика записей отображаются корректно.

---

## 📱 Сценарии использования

### Сценарий 1: Создание задачи

```
Пользователь: /start
Бот: Добро пожаловать! [🆕 Новая задача] [📋 Мои задачи]

Пользователь: 🆕 Новая задача
Бот: Выберите отдел [🚀 Развитие] [📢 Маркетинг] [🎨 Дизайн]

Пользователь: 🚀 Отдел Развития
Бот: По какому филиалу задача?

Пользователь: Королёв, ТЦ Юбилейный
Бот: Введите краткое название задачи

Пользователь: Обновить цены в филиале
Бот: Опишите задачу подробнее

Пользователь: Нужно обновить прайс-лист...
Бот: Превью задачи... [💬 Добавить комментарий] [✅ Продолжить]

Пользователь: ✅ Продолжить
Бот: Прикрепить файлы? [📎 Прикрепить] [⏭ Пропустить]

Пользователь: ⏭ Пропустить
Бот: ✅ Задача #39802 создана! (26.11.2025 22:30)
```

### Сценарий 2: Просмотр задач

```
Пользователь: 📋 Мои задачи
Бот: 
📋 Ваши задачи в работе (3)

🚀 Отдел Развития
  🆕 Новая задача:
    • #39802 — [Королёв] Обновить цены • 26.11.2025 22:30
  ⏳ Выполняются:
    • #39788 — [Москва] Ремонт кресла • 26.11.2025 22:08

[❌ Отменить задачу] [📋 Показать все] [🏠 Главное меню]
```

### Сценарий 3: Отмена задачи

```
Пользователь: ❌ Отменить задачу
Бот: Введите номер задачи (например: 39802)

Пользователь: 39802
Бот: ⚠️ Подтвердите отмену задачи #39802 — [Королёв] Обновить цены
     [✅ Да, отменить] [❌ Нет]

Пользователь: ✅ Да, отменить
Бот: ✅ Задача #39802 отменена
```

---

## ⚙️ Технические детали

### Стек технологий

| Компонент | Технология |
|-----------|------------|
| Язык | Python 3.11+ |
| Telegram | aiogram 3.x |
| HTTP-клиент | httpx |
| База данных | PostgreSQL + SQLAlchemy (asyncpg) |
| Миграции | Alembic |
| Админ-панель | FastAPI + Jinja2 |
| Конфигурация | python-dotenv |
| Хостинг | Beget VPS (Ubuntu) |

### FSM (Finite State Machine)

Бот использует FSM для управления диалогами:

**NewTaskStates:**
- `waiting_for_department` — выбор отдела
- `waiting_for_branch` — ввод филиала
- `waiting_for_title` — ввод названия
- `waiting_for_description` — ввод описания
- `waiting_for_confirm` — подтверждение
- `waiting_for_comment` — добавление комментария
- `waiting_for_files_choice` — выбор прикрепления файлов
- `waiting_for_files` — загрузка файлов

**CancelTaskStates:**
- `waiting_for_task_id` — ввод номера задачи
- `waiting_for_confirm` — подтверждение отмены

**RegistrationStates:**
- `waiting_for_full_name` — ввод ФИО
- `waiting_for_phone` — запрос контакта
- `waiting_for_branch_text` — ввод филиала
- `waiting_for_add_more_branches` — добавление ещё филиалов

**AddBranchStates:**
- `waiting_for_branch_text` — ввод нового филиала (для верифицированных)

### Идентификация пользователя

Задачи привязываются к пользователю через `TG_USER_ID` в описании задачи:
```
TG_USER_ID: 123456789
```

При просмотре задач бот фильтрует по этому полю.

### Кэширование

Этапы Kanban кэшируются в памяти для уменьшения запросов к Bitrix:
```python
_stages_cache: dict[str, dict[str, str]] = {}
```

---

## 📝 Логирование

Логи сохраняются в `logs/bot.log`:
- Запросы к Bitrix API
- Создание/отмена задач
- Ошибки

Просмотр логов:
```bash
tail -f logs/bot.log
```

---

## 🔄 Обновление документации

При добавлении новых функций обновляйте:
1. Раздел «Возможности бота»
2. Раздел «Сценарии использования»
3. Раздел «Технические детали» (если изменилась архитектура)

---

## 📞 Контакты

**Разработка:** [VGUROV050](https://github.com/VGUROV050)

---

*Последнее обновление: 28.11.2025*



