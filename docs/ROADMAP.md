# 🗺 Roadmap: Улучшения производительности и стабильности

Этот документ содержит план улучшений проекта BORODACH Franchise Bot.

---

## 🔴 Критично (Приоритет 1)

### 1. Retry-логика для API запросов
**Статус:** ✅ Выполнено  
**Оценка:** 1-2 часа

Добавить автоматические повторы при ошибках API (Bitrix, YClients, Telegram):

```python
from tenacity import retry, stop_after_attempt, wait_exponential

@retry(stop=stop_after_attempt(3), wait=wait_exponential(multiplier=1, min=2, max=10))
async def api_call():
    ...
```

**Файлы для изменения:**
- `yclients/client.py`
- `bitrix/client.py`
- `admin/routes.py` (send_telegram_notification)

---

### 2. Connection pooling для БД
**Статус:** ✅ Выполнено  
**Оценка:** 30 мин

Настроить пул соединений для PostgreSQL:

```python
engine = create_async_engine(
    DATABASE_URL,
    pool_size=10,
    max_overflow=20,
    pool_recycle=3600,
    pool_pre_ping=True,
)
```

**Файлы для изменения:**
- `database/connection.py`

---

### 3. Graceful shutdown
**Статус:** ✅ Выполнено  
**Оценка:** 1 час

Корректное завершение при перезапуске:

```python
async def on_shutdown(dp: Dispatcher):
    await dp.storage.close()
    await bot.session.close()
    scheduler.shutdown(wait=True)
```

**Файлы для изменения:**
- `main.py`

---

### 4. Health check endpoint
**Статус:** ✅ Выполнено  
**Оценка:** 30 мин

Эндпоинт для мониторинга состояния сервиса:

```python
@router.get("/health")
async def health_check():
    return {"status": "ok", "db": "connected", "uptime": ...}
```

**Файлы для изменения:**
- `admin/routes.py`

---

## 🟡 Важно (Приоритет 2)

### 5. Redis для кэширования
**Статус:** ✅ Выполнено  
**Оценка:** 3-4 часа

Кэширование рейтинга сети и других редко меняющихся данных:

- Установить Redis на сервер
- Добавить `aioredis` в зависимости
- Кэшировать `get_all_network_ratings()` на 1 час
- Кэшировать `get_all_yclients_companies()` на 15 минут

**Зависимости:**
```
aioredis>=2.0.0
```

---

### 6. Структурированное логирование
**Статус:** ✅ Выполнено  
**Оценка:** 2 часа

Замена стандартного logging на structlog:

```python
import structlog
logger = structlog.get_logger()
logger.info("task_created", task_id=123, user_id=456)
```

**Зависимости:**
```
structlog>=23.0.0
```

---

### 7. Rate limiting
**Статус:** ✅ Выполнено  
**Оценка:** 1-2 часа

Защита от спама в боте и лимитов API:

- Middleware для aiogram (1 msg/sec на пользователя)
- Throttling для YClients API

---

### 8. Валидация данных (Pydantic)
**Статус:** ✅ Выполнено  
**Оценка:** 2-3 часа

Pydantic модели для API ответов:

```python
class YClientsAnalytics(BaseModel):
    income_total: float
    avg_check: float
```

---

## 🟢 Желательно (Приоритет 3)

### 9. Unit-тесты
**Статус:** ⏳ Запланировано  
**Оценка:** 1-2 дня

Покрытие критичной логики тестами:

```
tests/
├── test_bitrix_tasks.py
├── test_yclients_client.py
├── test_registration_flow.py
└── conftest.py
```

**Зависимости:**
```
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
```

---

### 10. Миграции в CI/CD
**Статус:** ✅ Выполнено (06.12.2024)  
**Оценка:** 30 мин

Автоматическое применение миграций при деплое:
- Добавлен `alembic upgrade head` в `scripts/run.sh`
- Добавлена проверка миграций в GitHub Actions `deploy.yml`

---

### 11. Sentry для мониторинга ошибок
**Статус:** ✅ Выполнено (06.12.2024)  
**Оценка:** 1 час

Автоматический сбор ошибок с контекстом:
- Интеграция в `main.py` при наличии `SENTRY_DSN`
- AsyncioIntegration и SqlalchemyIntegration
- Переменная `ENVIRONMENT` для разделения prod/dev

**Зависимости:**
```
sentry-sdk>=1.39.1
```

---

### 12. Prometheus метрики
**Статус:** ✅ Выполнено (06.12.2024)  
**Оценка:** 2-3 часа

Метрики производительности:
- Модуль `utils/metrics.py` с Counter, Histogram, Gauge
- Эндпоинт `/metrics` для Prometheus scraping
- Middleware собирает метрики сообщений и времени обработки

**Метрики:**
- `telegram_messages_total` — счётчик сообщений по типам
- `message_processing_seconds` — время обработки
- `api_requests_total` — запросы к API
- `errors_total` — счётчик ошибок

**Зависимости:**
```
prometheus-client>=0.19.0
```

---

## 🔮 Будущие улучшения (Бэклог)

### 13. 🧠 AI Knowledge Base — Умный ассистент с базой знаний
**Статус:** 💡 Идея (Высокий приоритет)  
**Оценка:** 2-3 недели

Полноценный AI-ассистент, который знает все материалы из онлайн-института (100+ видеоуроков) и может анализировать данные YClients.

**Архитектура:**
```
Вопрос пользователя
        ↓
   AI Orchestrator (GPT-4)
        ↓
┌───────────────┬────────────────┐
│ 📚 RAG Search │ 📊 YClients    │
│   (pgvector)  │   Functions    │
│               │                │
│ • 100+ уроков │ • get_revenue  │
│ • Сценарии    │ • get_trends   │
│ • Best pract. │ • compare      │
└───────────────┴────────────────┘
        ↓
Персонализированный ответ
```

**Технологии:**
- **pgvector** — расширение PostgreSQL для vector embeddings
- **OpenAI text-embedding-3** — создание embeddings из текстов курсов
- **GPT-4o** — генерация ответов с контекстом
- **Function Calling** — запросы к YClients в реальном времени

**Примеры использования:**
```
Пользователь: "У меня упала выручка на 15%, что делать?"

AI: 📊 Анализ данных:
    Выручка: 1.2М → 1.02М (-15%)
    Средний чек: 2100₽ → 1950₽
    
    📚 Из курса "Управление выручкой":
    1. Проверьте загрузку мастеров (урок 24)
    2. Запустите акцию возврата клиентов (урок 45)
    
    🎯 Для вашего салона:
    • Топ-мастер в отпуске — это 20% выручки
    • Рекомендация: акция "Скидка 10% новым"
```

**План реализации:**

| Этап | Задача | Время |
|------|--------|-------|
| 1 | Подготовка текстов курсов (chunks) | 2-3 дня |
| 2 | Установка pgvector в PostgreSQL | 1 день |
| 3 | Создание embeddings и загрузка | 1 день |
| 4 | RAG-поиск по базе знаний | 2-3 дня |
| 5 | Function Calling для YClients | 2 дня |
| 6 | Интеграция в бот | 2 дня |
| 7 | Тестирование и доработка | 3-5 дней |

**Входные данные (нужны от бизнеса):**
- [ ] Тексты/сценарии всех уроков (txt, docx, pdf)
- [ ] Структура курсов (темы, модули)
- [ ] Ключевые термины и определения

**Файлы для создания:**
- `bot/knowledge_base.py` — RAG-поиск
- `bot/ai_functions.py` — YClients function calling
- `database/embeddings.py` — работа с pgvector
- `scripts/load_courses.py` — загрузка курсов в БД

**Стоимость:**
- Embeddings: ~$1-5 (единоразово)
- GPT-4o запросы: ~$0.01-0.05 за запрос
- Итого: ~$50-100/мес при активном использовании

---

### 15. Улучшение безопасности
**Статус:** ✅ Выполнено (06.12.2024)  
**Оценка:** 1-2 часа

Реализовано в `admin/auth.py`:

- ✅ **Cookie безопасность** — `Secure`, `HttpOnly`, `SameSite=lax`
- ✅ **CSRF защита** — генерация и проверка токенов для сессий
- ✅ **Brute-force защита** — 5 попыток → блок 15 мин по IP
- ✅ **Функция `set_secure_cookie()`** — для безопасной установки cookies

---

### 16. Многопользовательская авторизация в админке
**Статус:** 💡 Идея  
**Оценка:** 4-6 часов

Реализовать систему пользователей с ролями:

**Роли:**
- **Суперадмин** — полный доступ ко всему
- **Администратор** — управление партнёрами, рассылки
- **Менеджер** — только просмотр статистики

**Функционал:**
- Таблица `admin_users` (email, password_hash, role)
- Регистрация/приглашение пользователей
- Разграничение прав по роутам
- Лог действий пользователей

**Файлы для изменения:**
- `admin/auth.py` — переработка аутентификации
- `database/models.py` — модель AdminUser
- `admin/routes.py` — декораторы проверки ролей

---

### 17. Улучшение качества RAG (база знаний)
**Статус:** ✅ Частично выполнено  
**Оценка:** 4-6 часов

Повышение точности ответов AI-ассистента:

**Выполнено:**

1. ✅ **Overlap (перекрытие)** — чанки перекрываются на 50 слов
   - Реализовано в `VideoProcessor.chunk_transcript()` с параметром `overlap_words=50`

2. ✅ **Summary chunks** — краткое содержание каждого урока (03.12.2025)
   - Скрипт `scripts/generate_summaries.py` для существующих уроков
   - Автогенерация при импорте в `import_module.py`
   - Summary chunk с `chunk_index=-1` для приоритетного поиска
   - Поле `summary` в модели `KnowledgeLesson`

3. ✅ **Увеличен контекст поиска** — limit 8 чанков, 800 символов превью

**Осталось (опционально):**

4. ✅ **Context Expansion (Parent-child эффект)** — Выполнено 06.12.2024
   - При поиске чанка автоматически подгружаются соседние чанки
   - Параметры `expand_context=True`, `context_window=1` в `search_chunks()`
   - Результат: поиск по точному чанку, но возврат расширенного контекста

5. 💡 **Metadata enrichment** (опционально)
   - Добавить теги/темы к каждому чанку
   - Фильтрация по теме перед поиском

**Достигнутый результат:**
- ✅ Overlap предотвращает разрыв мыслей
- ✅ Summaries улучшают релевантность поиска
- ✅ Больше контекста для AI
- ✅ Context Expansion даёт эффект parent-child без пересоздания чанков

---

### 18. Telegram Mini App для аналитики
**Статус:** 💡 Идея  
**Оценка:** 2-3 недели

Веб-приложение внутри Telegram для визуальной аналитики:

**Функционал:**
- 📊 **Дашборд** — выручка, средний чек, динамика за период
- 📈 **Графики** — тренды по месяцам, сравнение с сетью
- 🗺️ **Карта салонов** — география присутствия
- 🏆 **Рейтинг сети** — интерактивная таблица с фильтрами
- 📅 **Календарь** — загрузка мастеров, записи

**Технологии:**
- **Frontend:** React/Vue + Tailwind CSS
- **Backend:** FastAPI (существующий)
- **Telegram:** WebApp API
- **Графики:** Chart.js / Recharts

**Преимущества:**
- Красивая визуализация данных
- Удобный UX для сложных отчётов
- Работает прямо в Telegram (без отдельного приложения)

**Файлы для создания:**
- `webapp/` — React-приложение
- `admin/api/` — REST API для Mini App
- Интеграция с кнопкой в боте

---

## 📅 План внедрения

| Этап | Задачи | Срок |
|------|--------|------|
| **Фаза 1** | Retry, Pool, Graceful shutdown, Health | 1 неделя |
| **Фаза 2** | Redis кэш, Логи | 1 неделя |
| **Фаза 3** | Rate limit, Валидация | 1 неделя |
| **Фаза 4** | Тесты, Sentry, Метрики | 2 недели |

---

## ✅ Выполнено

### Приоритет 1 (Критично)
- **Retry-логика для API** (29.11.2025) — tenacity декоратор для Bitrix/YClients
- **Connection pooling для БД** (29.11.2025) — настроен пул соединений PostgreSQL
- **Graceful shutdown** (29.11.2025) — корректное завершение при SIGINT/SIGTERM
- **Health check endpoint** (29.11.2025) — добавлен `/health` для мониторинга

### Приоритет 2 (Важно)
- **Redis кэширование** (29.11.2025) — модуль cache/ с TTL для рейтинга и компаний
- **Структурированные логи** (29.11.2025) — structlog с JSON/console форматом
- **Rate limiting** (29.11.2025) — middleware для защиты от спама (0.5 сек)
- **Pydantic валидация** (29.11.2025) — schemas/ для YClients и Bitrix API

---

*Создано: 29.11.2025*

